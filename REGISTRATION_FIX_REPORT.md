# 新規登録エラー修正レポート

## 発見された問題

### 1. 環境変数の動的読み込み問題
- フロントエンドのregister.jsとlogin.jsで`process.env.NEXT_PUBLIC_API_URL`を実行時に読み込んでいたが、Next.jsでは環境変数はビルド時に置き換えられるため、実行時には正しく解決されない
- これにより、APIエンドポイントのURLが`undefined`になり、リクエストが失敗していた

### 2. エラーレスポンス形式の不一致
- バックエンドのエラーハンドリングミドルウェアが`{"error": {"message": "...", "type": "..."}}`形式でエラーを返すが、フロントエンドは`{"detail": "..."}`形式を期待していた
- エラーメッセージが正しく表示されず、常に「登録に失敗しました」という汎用メッセージが表示されていた

### 3. 不要なCORS設定
- register.jsで`credentials: 'include'`が設定されていたが、現在の実装では不要であり、場合によってはCORS問題を引き起こす可能性があった

## 実施した修正

### ファイル: /frontend/components/register.js
- `import { getApiEndpoint } from '../services/env-config';`を追加
- `process.env.NEXT_PUBLIC_API_URL`の直接参照を`getApiEndpoint()`関数呼び出しに変更
- エラーレスポンスの処理を改善し、新旧両方の形式（`error.message`と`detail`）に対応
- デバッグ用のコンソールログを追加（API URL、エラーステータス、エラー内容）
- `credentials: 'include'`オプションを削除

### ファイル: /frontend/components/login.js
- `import { getApiEndpoint } from '../services/env-config';`を追加
- `API_BASE_URL`の定義を関数内に移動し、`getApiEndpoint()`から取得するように変更
- エラーレスポンスの処理を改善し、新しい形式（`error.message`）にも対応

### ファイル: /backend/main.py
- 登録エンドポイントにログ出力を追加（`logger.info`）
- 登録成功時とエラー時の詳細なログを追加

## テスト結果

### 成功したテスト
- 新規メールアドレスでの登録 ✅
- 登録後のアクセストークン取得 ✅
- 既存メールアドレスでの登録拒否（重複チェック） ✅
- エラーメッセージの正しい表示 ✅
- ログイン機能の動作確認 ✅

### 動作確認方法
1. Docker環境での動作確認
   - `docker-compose -f docker-compose.development.yml ps`で全サービスが起動していることを確認
   - バックエンドAPI（http://localhost:8000）が正常に応答
   - フロントエンド（http://localhost:3000）が正常に動作

2. curlコマンドでのAPI直接テスト
   - 新規登録エンドポイントが正常に動作
   - 適切なJWTトークンが返却される

## 追加で実装した機能
- 詳細なデバッグログ機能
  - API URLの確認
  - エラーレスポンスの詳細表示
  - 登録試行時のペイロード確認

- エラーハンドリングの改善
  - 複数のエラーレスポンス形式に対応
  - JSONパースエラー時のフォールバック処理

## 今後の改善提案

### 1. エラーメッセージの統一
- バックエンドとフロントエンドでエラーレスポンスの形式を統一すべき
- OpenAPIスキーマなどを使用して、API仕様を明確に定義することを推奨

### 2. 環境設定の改善
- 環境変数の管理方法を改善し、開発/本番環境の切り替えをより明確にする
- `.env.example`ファイルを作成し、必要な環境変数を文書化する

### 3. ユーザーエクスペリエンスの向上
- 登録時のローディング表示の改善
- エラー時の再試行ボタンの追加
- パスワード強度のリアルタイム表示

### 4. セキュリティの強化
- パスワードの最小要件の強化（現在は8文字以上のみ）
- レート制限の実装
- CSRF対策の実装（必要に応じて）

### 5. テストの自動化
- E2Eテストの追加（Cypress、Playwrightなど）
- APIの統合テストの追加
- CI/CDパイプラインでの自動テスト実行